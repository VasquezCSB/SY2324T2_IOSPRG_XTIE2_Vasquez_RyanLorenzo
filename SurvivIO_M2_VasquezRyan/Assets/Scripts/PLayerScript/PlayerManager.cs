using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using TMPro;

//This script will handle UI for player stuff such as Health and Ammo
public class PlayerManager : MonoBehaviour
{

    //public TMP_Text clip;
    //public TextMeshProUGUI reloadClip;
    //public Shooting shooting;

    // Start is called before the first frame update
    void Start()
    {
        //clip.text = shooting.currentClip.ToString();
        //reloadClip.text = shooting.maxAmmoSize.ToString();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
